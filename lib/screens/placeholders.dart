import 'package:flutter/material.dart';

class ContentModuleScreen extends StatelessWidget {
  const ContentModuleScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(appBar: AppBar(title: const Text('Content Module Placeholder')));
  }
}

class CodeFillGameScreen extends StatelessWidget {
  const CodeFillGameScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(appBar: AppBar(title: const Text('Code Fill Game Placeholder')));
  }
}

class LogicBuildingScreen extends StatelessWidget {
  const LogicBuildingScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(appBar: AppBar(title: const Text('Logic Building Placeholder')));
  }
}
